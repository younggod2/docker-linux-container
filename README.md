# 🐧 Linux Learning Lab - Docker Container

Контейнер Docker для изучения Linux в изолированной среде на базе Ubuntu 24.04.

## 📖 Описание

Этот проект предоставляет готовую среду для изучения Linux с предустановленными инструментами и настроенным Zsh с Oh My Zsh. Идеально подходит для практики команд Linux без риска сломать основную систему!

## ✨ Возможности

- 🖥️ **Ubuntu 24.04 LTS** — свежая и стабильная база
- 🇷🇺 **Русская локаль** — полная поддержка кириллицы
- 🐚 **Zsh + Oh My Zsh** — красивый терминал с автодополнением и подсветкой синтаксиса
- 📚 **Man-страницы** — документация работает из коробки (`man ls`, `whatis curl`)
- 📦 **apt install без apt update** — устанавливайте пакеты сразу!

### 🛠️ Предустановленные инструменты

| Категория | Инструменты |
|-----------|-------------|
| Системные | `htop`, `tree`, `less`, `sudo` |
| Редакторы | `vim`, `nano` |
| Сеть | `curl`, `wget`, `ping`, `net-tools`, `iproute2` |
| Разработка | `git`, `gawk` |
| Документация | `man-db`, `manpages` |

## 🚀 Быстрый старт

### Сборка и запуск

```bash
docker-compose up -d --build
```

### Подключение к контейнеру

```bash
docker exec -it linux-learning-lab zsh
```

или

```bash
docker-compose exec linux-lab /bin/zsh
```

### Остановка

```bash
docker-compose down
```

### Полная очистка (с удалением данных)

```bash
docker-compose down -v
```

## 📁 Структура проекта

```
.
├── 🐳 Dockerfile           # Определение образа
├── 📋 docker-compose.yml   # Конфигурация Docker Compose
├── ⚙️ .zshrc.local         # Настройки Zsh
├── 📂 workspace/           # Рабочая директория (сохраняется)
└── 📖 README.md            # Этот файл
```

## 💾 Персистентность данных

| Что сохраняется | Где хранится | Volume |
|-----------------|--------------|--------|
| Ваши файлы | `workspace/` | Монтируется из хоста |
| История команд, настройки | `/root` | `linux-home` |

Данные **не теряются** при перезапуске или пересборке контейнера!

## 🎮 Попробуйте!

После входа в контейнер:

```bash
# Документация
man ls
whatis curl

# Установите что-нибудь забавное
apt install -y cowsay sl fortune-mod

# Попробуйте
cowsay "Привет, Linux!"
fortune | cowsay
sl
```

## ⚙️ Настройка

### Добавление пакетов в образ

Отредактируйте `Dockerfile`, добавьте пакеты в секцию `apt-get install` и пересоберите:

```bash
docker-compose build --no-cache
docker-compose up -d
```

### Изменение конфигурации Zsh

Отредактируйте `.zshrc.local` и пересоберите образ.

## 📝 Примечания

- 🍎 Оптимизирован для **Apple Silicon** (arm64), но работает и на x86
- 🎯 Контейнер автоматически перезапускается при падении
- 📄 Файл `.zshrc.local` содержит macOS-специфичные функции — они не мешают работе в Linux
